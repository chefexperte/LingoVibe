# GitHub OAuth Configuration
# Get these values by creating a GitHub OAuth App at:
# https://github.com/settings/developers

# Your GitHub OAuth App Client ID (REQUIRED - public, safe to commit)
# This must be set for the app to work!
VITE_GITHUB_CLIENT_ID=your_github_client_id_here

# OAuth Redirect URI (must match your OAuth app settings)
# For local development:
# VITE_REDIRECT_URI=http://localhost:5173/LingoVibe/auth/callback
# For production:
# VITE_REDIRECT_URI=https://yourusername.github.io/LingoVibe/auth/callback
VITE_REDIRECT_URI=http://localhost:5173/LingoVibe/auth/callback

# OAuth Proxy URL (required for static sites)
# Option 1: Use the default proxy (free but may have rate limits)
VITE_OAUTH_PROXY_URL=https://github-oauth-proxy.herokuapp.com/authenticate
# Option 2: Deploy your own proxy (recommended for production)
# VITE_OAUTH_PROXY_URL=https://your-proxy.herokuapp.com/authenticate
